services:
  redis:
    image: redis:7
    ports:
      - "6379:6379"

  bot:
    build: ./bot
    env_file:
      - .env
    depends_on:
      - redis

  worker:
    build: ./worker
    env_file:
      - .env
    depends_on:
      - redis
